# M2 Primitive Geometry Decisions

## Implementation plan

1. Add UUID metadata, a mutation guard, and one shared result envelope.
2. Add managed, bounded primitive creation using central mm/degrees validation.
3. Add resolver-based extrusion, follow-me, transforms, and conservative solid booleans.
4. Expose tools through MCP with annotations and capability checks; add Ruby, Python, and cross-language tests.
5. Add disposable-model smoke instructions, update docs/CI, run tests and package the RBZ.

## Contract choices

- All M2 points are world-coordinate `[x_mm, y_mm, z_mm]`. Angles are degrees. Conversion to/from SketchUp units stays in `HomeCAD::Units`.
- Every create tool creates a root Group container and stores geometry inside it, including a single edge or face.
- HomeCAD identity is an RFC 4122 UUID generated by Ruby's `SecureRandom.uuid`, stored with type, schema version, revision, and generated flag in the `HomeCAD` attribute dictionary. Inspection continues to avoid writing metadata.
- Every mutation is wrapped in one `HomeCAD::Operation`; errors abort. Shared target resolution and mutation policy reject missing/ambiguous, invalid, locked, nested-root, and generated non-primitive targets as applicable.
- `push_pull` and `follow_me` only edit faces in a root Group. Shared Group definitions are rejected to avoid silently changing all placements. Nested edit contexts are not supported in M2.
- Boolean operands must be distinct root-level manifold Groups or ComponentInstances. Inputs are duplicated, boolean work is done on copies, and the result is a newly identified object; source identities and source geometry remain untouched. `union`/`intersect` use official Group methods. `difference` uses documented `Group#trim` to mean target minus tool because official `subtract` receiver/argument prose conflicts. API availability is checked at runtime.
- Primitive tools remain low-level fallback capabilities. No future domain object types or `eval_ruby` are exposed.

## Research references

Reviewed repository commits (no code copied):

- `Tarkiin/SketchUp-MCP` — `a47ca45d9568f175d0d2aed5320713fb3b272e37`, MIT; primitive creation patterns.
- `zinin/sketchup-mcp2` — `70c6edb50f4edbeaacdda1726ce93c4ba46fc1c0`, MIT; geometry builders and mutation/test patterns.
- `mhyrr/sketchup-mcp` — `aa096f04d3d7b22a70860368f2b576343feac405`; README identifies MIT; boolean API usage. No code copied.
- `SidhNor/sketchup-mcp-server` was not used as an implementation source; no AGPL code was copied.
- Official [SketchUp Group API](https://ruby.sketchup.com/Sketchup/Group.html), [Entities API](https://ruby.sketchup.com/Sketchup/Entities.html), [Face API](https://ruby.sketchup.com/Sketchup/Face.html), and [Model API](https://ruby.sketchup.com/Sketchup/Model.html) are the source of truth.

## Known manual checks

Standalone tests use SketchUp API doubles. In the real SketchUp version, verify each primitive's geometry, rollback after an induced geometry failure, follow-me behavior with a valid path, the supported boolean methods on manifold solids, source preservation, exactly one Undo per call, and the two-step `scripts/smoke_m2.py` flow. The smoke must use a disposable/test model.
